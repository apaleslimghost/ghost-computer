<!DOCTYPE html>
<html>
    <head>
        <title>GhostComputer</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>

        <%= stylesheet_link_tag 'application', media: 'all' %>
    </head>

    <body class="<%= controller_name.underscore.dasherize %>">
        <%= render 'nav' %>

        <main>
            <%= yield %>
        </main>
    </body>
</html>
